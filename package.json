{
  "name": "share-schema",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "eslint": "^8.1.0",
    "eslint-plugin-simple-import-sort": "^7.0.0",
    "fs-extra": "^10.0.0",
    "rimraf": "^3.0.2",
    "tsc-alias": "^1.4.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "firebase": "^9.2.0",
    "firebase-admin": "^10.0.0"
  },
  "scripts": {
    "prebuild": "rimraf lib",
    "build": "yarn build:web && yarn build:admin",
    "build:web": "tsc && tsc-alias",
    "prebuild:admin": "node ./pre-build-admin.js",
    "build:admin": "tsc && tsc-alias",
    "postbuild:admin": "node ./post-build-admin.js"
  }
}
